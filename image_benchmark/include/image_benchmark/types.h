#pragma once

#include <string>
#include <utility>
#include <vector>

namespace image_benchmark {

using Pair = std::pair<std::string, std::string>;
using KVContainer = std::vector<Pair>;

}  // namespace image_benchmark
